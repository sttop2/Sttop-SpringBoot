# tomcat
server:
  port: 8081

# 环境配置
spring:
#  profiles:
#    active: dev

# 数据库
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/sttopspringboot?characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

# 重定向
  thymeleaf:
    suffix: .ftl

# 静态资源文件处理
  mvc:
    static-path-pattern: /static/**
  resources:
    static-locations: classpath:/